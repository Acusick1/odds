name: CI/CD Pipeline

on:
  push:
    branches: [master]
  workflow_dispatch:

jobs:
  test:
    name: Run Tests
    uses: ./.github/workflows/test.yml
    secrets: inherit

  deploy-dev:
    name: Deploy to Dev
    needs: test
    uses: ./.github/workflows/deploy-dev.yml
    secrets: inherit
